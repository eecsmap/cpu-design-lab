# Measurements

One file per experiment. Every file records the Chipyard release, the submodule
commit, the workload, and the exact method — so a number here can always be
re-derived or challenged later.

A measurement without its provenance becomes folklore within a few months. If
you cannot say which commit produced a number, do not write the number down.
