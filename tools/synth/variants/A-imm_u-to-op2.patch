diff --git a/src/main/scala/sodor/rv32_1stage/consts.scala b/src/main/scala/sodor/rv32_1stage/consts.scala
index 4ab7df8..c09b4ac 100644
--- a/src/main/scala/sodor/rv32_1stage/consts.scala
+++ b/src/main/scala/sodor/rv32_1stage/consts.scala
@@ -48,7 +48,7 @@ trait ScalarOpConstants
 
    // RS1 Operand Select Signal
    val OP1_RS1 = 0.asUInt(2.W) // Register Source #1
-   val OP1_IMU = 1.asUInt(2.W) // immediate, U-type
+   val OP1_PC  = 1.asUInt(2.W) // PC
    val OP1_IMZ = 2.asUInt(2.W) // Zero-extended rs1 field of inst, for CSRI instructions
    val OP1_X   = 0.asUInt(2.W)
 
@@ -56,7 +56,7 @@ trait ScalarOpConstants
    val OP2_RS2 = 0.asUInt(2.W) // Register Source #2
    val OP2_IMI = 1.asUInt(2.W) // immediate, I-type
    val OP2_IMS = 2.asUInt(2.W) // immediate, S-type
-   val OP2_PC  = 3.asUInt(2.W) // PC
+   val OP2_IMU = 3.asUInt(2.W) // immediate, U-type
    val OP2_X   = 0.asUInt(2.W)
 
    // Register File Write Enable Signal
@@ -76,6 +76,7 @@ trait ScalarOpConstants
    val ALU_SLT = 9.asUInt(4.W)
    val ALU_SLTU= 10.asUInt(4.W)
    val ALU_COPY1= 11.asUInt(4.W)
+   val ALU_COPY2= 12.asUInt(4.W)
    val ALU_X   = 0.asUInt(4.W)
 
    // Writeback Select Signal
diff --git a/src/main/scala/sodor/rv32_1stage/cpath.scala b/src/main/scala/sodor/rv32_1stage/cpath.scala
index 86e6538..cc250cf 100644
--- a/src/main/scala/sodor/rv32_1stage/cpath.scala
+++ b/src/main/scala/sodor/rv32_1stage/cpath.scala
@@ -60,8 +60,8 @@ class CtlPath(implicit val conf: SodorCoreParams) extends Module
                   SB      -> List(Y, BR_N  , OP1_RS1, OP2_IMS , ALU_ADD ,  WB_X  , REN_0, MEN_1, M_XWR, MT_B,  CSR.N),
                   SH      -> List(Y, BR_N  , OP1_RS1, OP2_IMS , ALU_ADD ,  WB_X  , REN_0, MEN_1, M_XWR, MT_H,  CSR.N),
 
-                  AUIPC   -> List(Y, BR_N  , OP1_IMU, OP2_PC  , ALU_ADD ,  WB_ALU, REN_1, MEN_0, M_X ,  MT_X,  CSR.N),
-                  LUI     -> List(Y, BR_N  , OP1_IMU, OP2_X   , ALU_COPY1, WB_ALU, REN_1, MEN_0, M_X ,  MT_X,  CSR.N),
+                  AUIPC   -> List(Y, BR_N  , OP1_PC , OP2_IMU , ALU_ADD ,  WB_ALU, REN_1, MEN_0, M_X ,  MT_X,  CSR.N),
+                  LUI     -> List(Y, BR_N  , OP1_X  , OP2_IMU , ALU_COPY2, WB_ALU, REN_1, MEN_0, M_X ,  MT_X,  CSR.N),
 
                   ADDI    -> List(Y, BR_N  , OP1_RS1, OP2_IMI , ALU_ADD ,  WB_ALU, REN_1, MEN_0, M_X  , MT_X,  CSR.N),
                   ANDI    -> List(Y, BR_N  , OP1_RS1, OP2_IMI , ALU_AND ,  WB_ALU, REN_1, MEN_0, M_X  , MT_X,  CSR.N),
diff --git a/src/main/scala/sodor/rv32_1stage/dpath.scala b/src/main/scala/sodor/rv32_1stage/dpath.scala
index 69e8a47..3fdac6b 100644
--- a/src/main/scala/sodor/rv32_1stage/dpath.scala
+++ b/src/main/scala/sodor/rv32_1stage/dpath.scala
@@ -153,13 +153,13 @@ class DatPath(implicit val p: Parameters, val conf: SodorCoreParams) extends Mod
 
    val alu_op1 = MuxCase(0.U, Array(
                (io.ctl.op1_sel === OP1_RS1) -> rs1_data,
-               (io.ctl.op1_sel === OP1_IMU) -> imm_u_sext,
+               (io.ctl.op1_sel === OP1_PC)  -> pc_reg,
                (io.ctl.op1_sel === OP1_IMZ) -> imm_z
                )).asUInt
 
    val alu_op2 = MuxCase(0.U, Array(
                (io.ctl.op2_sel === OP2_RS2) -> rs2_data,
-               (io.ctl.op2_sel === OP2_PC)  -> pc_reg,
+               (io.ctl.op2_sel === OP2_IMU) -> imm_u_sext,
                (io.ctl.op2_sel === OP2_IMI) -> imm_i_sext,
                (io.ctl.op2_sel === OP2_IMS) -> imm_s_sext
                )).asUInt
@@ -182,7 +182,8 @@ class DatPath(implicit val p: Parameters, val conf: SodorCoreParams) extends Mod
                   (io.ctl.alu_fun === ALU_SLL)  -> ((alu_op1 << alu_shamt)(conf.xprlen-1, 0)).asUInt,
                   (io.ctl.alu_fun === ALU_SRA)  -> (alu_op1.asSInt >> alu_shamt).asUInt,
                   (io.ctl.alu_fun === ALU_SRL)  -> (alu_op1 >> alu_shamt).asUInt,
-                  (io.ctl.alu_fun === ALU_COPY1)-> alu_op1
+                  (io.ctl.alu_fun === ALU_COPY1)-> alu_op1,
+                  (io.ctl.alu_fun === ALU_COPY2)-> alu_op2
                   ))
 
    // Branch/Jump Target Calculation
